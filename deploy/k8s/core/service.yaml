apiVersion: v1
kind: Service
metadata:
  name: core-api
  namespace: core-staging
  labels:
    app: core-api
spec:
  type: ClusterIP
  selector:
    app: core-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
